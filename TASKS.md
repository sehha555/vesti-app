# Vesti 任務進度與待辦

> 最後更新：2026-09-26
> 所有改動都在分支 `claude/resume-ot3fvx`，**還沒合進 `master`**。
> 接手的人（或 Claude）請先讀這份，再讀 `docs/legal/link-out-compliance.md`。

---

## 一句話現況

App 的主流程已經串起來了：**放衣服進衣櫃 → AI 認出這是什麼衣服 → AI 依天氣搭配 → 使用者說「要／不要」→ 下次推薦參考這些回饋**。
但這個流程**還沒用真的帳號和真的衣服跑過**（開發環境沒有 AI 金鑰，也沒有測試用資料庫），所以下一步最重要的是「真的跑一次」。

---

## 回家後第一步

在自己電腦的專案資料夾執行：

```bash
git fetch origin
git checkout claude/resume-ot3fvx
git pull
npm install
npm test            # 應該全部通過（目前 505 個）
```

然後跟 Claude 說：「讀 TASKS.md，接著做」。

---

## 只有你能做的事（要帳號、金鑰、錢或律師）

依重要程度排序。打勾代表做完了。

- [ ] **1. 開一個「開發用」的 Supabase 專案**（免費）
  - 為什麼：現在只有正式環境一個資料庫，測試會弄髒正式資料。有開發專案，Claude 才能幫你灌測試衣服、試跑整個流程。
  - 怎麼做：到 supabase.com 按 New project，名字取 `vesti-dev`，建好後把網址和兩把金鑰（anon key、service role key）填進 `.env.local`。
- [ ] **2. 把資料庫更新套上去**（先套開發專案，沒問題再套正式）
  - 為什麼：這次新增了 3 個資料庫變更，沒套的話**上傳衣服會失敗**。
  - 怎麼做：`npm run supabase:db:push`
  - 會套上的 3 個檔案：
    - `20260925120000_tighten_open_rls_policies.sql`：關掉「任何人都能看別人資料」的漏洞
    - `20260925150000_outfit_feedback.sql`：存「要／不要／有沒有穿」的回饋
    - `20260926100000_closet_item_attributes.sql`：存 AI 認出的衣服屬性
- [ ] **3. 設定 Gemini 金鑰**（`GEMINI_API_KEY`）
  - 正式上線前要**改成付費方案**：免費方案的資料可能被 Google 拿去訓練，這跟隱私權政策衝突。
  - 現在每上傳一件衣服會多叫一次 Gemini，費用會增加一點。
- [ ] **4. 設定 `SUPABASE_SERVICE_ROLE_KEY`**：沒設的話「刪除帳號」會顯示無法刪除。
- [ ] **5. 補上法律頁面裡〔〕的空格**：公司名稱、聯絡信箱、資料放在哪個國家、管轄法院。
  改 `apps/web/app/legal/LegalPage.tsx`（公司名、信箱）和 `apps/web/app/legal/privacy/page.tsx`、`terms/page.tsx`。
- [ ] **6. 找律師看** `docs/legal/link-out-compliance.md` 最後的「要問律師的問題」，確認後拿掉頁面上的「草稿」提示。
- [ ] **7. 部署後設定** `VESTI_BOT_INFO_URL=https://你的網域/legal/bot`
- [ ] **8. 決定要不要開 PR 把分支合進 master**（要的話跟 Claude 說「開 PR」，它會開好並盯 CI）。

---

## 已經做完的（白話版）

| 項目 | 白話說明 |
|---|---|
| 登入／登出修好 | 以前 email 登入後，其他功能認不得你；登出按鈕也沒真的登出。現在都正常。 |
| 資料安全 | 以前有幾張表任何人都能讀寫，已關掉；每個人只能看自己的衣服。 |
| 衣櫃 | 可以拍照上傳、貼商品連結匯入、改名稱、刪除；會自動去背。 |
| 今日穿搭、收藏 | 以前存在瀏覽器、換手機就不見，現在存在伺服器。 |
| 回饋按鈕 | 推薦卡片有「👍 要這套」「👎 不要（可選原因）」，隔天會問「昨天有穿嗎」。這些都會餵給 AI。 |
| 購物改成外連 | 拿掉 App 內的購物車、結帳、付款。按「前往 uniqlo.com 購買 ↗」直接去官網，並標示可能有分潤（法規要求）。 |
| 抓商品圖守規矩 | 抓網頁時老實報上名字（VestiBot）、遵守網站的 robots.txt、蝦皮等不讓抓的網站直接跳過。 |
| 法律頁面 | 服務條款、隱私權政策、VestiBot 說明（草稿，待律師確認）。 |
| 刪除帳號 | 個人頁可以刪帳號，衣服照片、收藏、回饋紀錄一起刪掉。 |
| AI 認衣服 | 上傳時 AI 自動填：類別、顏色、花紋、保暖度 1–5、正式度 1–5、風格。舊衣服可以按「AI 辨識」補上。 |
| 挑衣服更聰明 | 以前只拿最新 30 件給 AI；現在先依天氣排除（熱天不給羽絨衣），再每類輪流挑，避免 30 件全是上衣。 |
| 自動檢查 | 每次推程式碼會自動跑 lint、型別檢查、測試。 |

---

## 接下來的任務（照順序做）

### T1. 用真的資料跑一次完整流程 ⭐ 最優先
- **前提**：你完成上面的 1、2、3。
- **要做**：上傳 10～20 件真的衣服 → 看 AI 認得準不準 → 看推薦合不合理 → 按幾次要／不要 → 隔天看推薦有沒有變。
- **要記下來的**：認錯的衣服（截圖）、覺得怪的搭配、太慢的地方。
- **為什麼重要**：到目前為止 AI 的部分都是「假裝的回應」測出來的，真的效果還沒人看過。

### T2. 測試衣櫃腳本
- **前提**：有開發用 Supabase（上面第 1 項）。
- **要做**：寫一個腳本，一次灌 50 件公開資料集的衣服照片（例如 Kaggle 的 Fashion Product Images）進測試帳號，讓你不用自己拍就能測推薦。
- **大小**：小，半天內。

### T3. 推薦成效統計
- **要做**：一頁簡單的數字：推薦被按「要」的比例、按「不要」最常見的原因、選了之後真的有穿的比例。
- **為什麼**：沒有這些數字，就不知道 T4 排序器有沒有讓推薦變好。
- **大小**：小。資料已經在 `outfit_feedback` 表裡，只是還沒整理出來看。

### T4. 排序器（核心循環的第 ④ 步）⏰ 排程：T1 跑完、累積約 2 週回饋後再做
- **白話說明**：現在是 AI 直接給 3 套，我們照單全收。排序器是讓 AI 先多給幾套（例如 6 套），再用**你過去的回饋**幫每套打分數，只留分數最高的 3 套。
  - 跟你按過「要」的搭配很像 → 加分
  - 含有你按過「不要」的組合、或違反你選的原因（例如「太正式」）→ 扣分
  - 最近 3 天穿過的整套 → 扣分
  - 正式度相差太大、不合天氣 → 扣分
- **開始的條件**：至少有 50 筆左右的回饋（大約一個人用兩週），不然分數是亂猜的。
- **大小**：中。先用簡單的加減分規則，不用訓練模型。
- **做完怎麼驗證**：比較 T3 的「被按要的比例」有沒有上升。

### T5. 把分支合進 master
- 開 PR、確認 CI 綠燈、合併。隨時可以做，越早越好，免得分支越改越大。

### T6. UNIQLO 匯入可能被擋
- 我們現在老實報上 VestiBot 的名字，UNIQLO 網站可能因此拒絕。**不要改回假裝成瀏覽器**（法律風險）。
- 被擋時的替代方案：請使用者拍照上傳；或申請 UNIQLO／聯盟行銷的官方商品資料。

### T7. 以後再說的技術債（不急）
- 樣式檔是 Figma 匯出時預先產生的，很多樣式不能用，只好到處寫 inline style。要從根本解決得重新設定 Tailwind。
- 首頁今天和昨天的穿搭各發一次請求，可以合成一次。
- `daily_outfit_plans.outfit_id` 存的只是卡片順序，沒意義，之後改掉。
- `services/reco` 裡還有一套舊的事件系統沒在用，可以清掉。
- 每次推薦都把最多 30 張圖送給 Gemini，貴又慢。衣服都辨識過屬性之後，可以只送文字、不送圖（T1 之後評估效果再決定）。

---

## 需要你決定的事

1. **排序器（T4）要不要做、什麼時候做**：建議等 T1、T3 有數字再決定。
2. **要不要開 PR 合進 master**（T5）。
3. **衣櫃照片傳到國外處理，要不要另外請使用者打勾同意**：目前只有文字提醒，加上註冊時同意隱私權政策。要問律師。
4. **正式上線的時間點**：決定了才能排法律和付費方案的時程。

---

## 給 Claude 的技術交接（這段可以跳過）

- 分支：`claude/resume-ot3fvx`。push 前跑 `npm run typecheck && npm run lint && npm test`，網頁改動再跑 `cd apps/web && npx next build`。
- `apps/web` 的 tsconfig 沒開 `strict`，union 型別靠 truthiness 收窄不了，所以回傳型別都用明確的 `null` 欄位（參考 `lib/http/require-user.ts`）。
- 預編譯的 CSS 缺很多 Tailwind class，新 UI 用 inline style 或 `var(--vesti-primary)`。
- 核心循環相關檔案：
  - ① 衣物理解：`lib/ai/tag-item.ts`、`lib/closet/attributes.ts`、`lib/closet/create-item.ts`、`app/api/closet-items/analyze`
  - ② 候選：`lib/reco/candidates.ts`
  - ③ 搭配（Gemini）：`lib/ai/suggest-outfits.ts`、`lib/ai/outfit-prompt.ts`
  - ④ 排序器：尚未實作。建議放在 `lib/reco/rank.ts`，輸入候選搭配與 `lib/feedback/summary.ts` 讀到的回饋，輸出排序後的前 3 套。
  - ⑤ 回饋：`app/api/reco/events`、`lib/feedback/*`、`app/components/figma/StackedCards.tsx`
- 一套搭配的識別碼是「單品 id 排序後用 | 串起來」（`lib/outfits/key.ts`），卡片 id 1/2/3 只是位置。
- 法遵細節與還沒處理的項目在 `docs/legal/link-out-compliance.md`。
