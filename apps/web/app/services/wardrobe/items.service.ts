/**
 * Wardrobe Items Service (stub)
 * Real implementation is in /services/wardrobe
 */

export const wardrobeServicePromise = Promise.resolve({
  getItemsByUserId: async (userId: string) => {
    // Stub implementation
    return [];
  },
});
